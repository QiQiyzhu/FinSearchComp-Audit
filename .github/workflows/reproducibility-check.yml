name: Reproducibility check

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "audit/**"
      - "advanced_rag/**"
      - "finagent_platform/**"
      - "requirements-platform.txt"
      - "reproduce.py"
      - "docs/REPRODUCIBILITY.md"
      - ".github/workflows/reproducibility-check.yml"
  push:
    branches-ignore: [main]
    paths:
      - "audit/**"
      - "advanced_rag/**"
      - "finagent_platform/**"
      - "requirements-platform.txt"
      - "reproduce.py"
      - "docs/REPRODUCIBILITY.md"
      - ".github/workflows/reproducibility-check.yml"

permissions:
  contents: read

jobs:
  reproduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Reproduce report and validate evidence structure
        # The reproduced platform demo imports the FastAPI application.
        # Keep this dependency explicit on a fresh GitHub runner.
        run: |
          python -m pip install -r requirements-platform.txt
          python reproduce.py --output build/site
      - name: Show generated artifacts
        run: |
          find build/site -maxdepth 1 -type f -printf "%f %s bytes\n" | sort
